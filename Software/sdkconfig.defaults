# Brownout: use lowest threshold (~2.44V) to prevent false triggers
# during boot image loading when flash current peaks
CONFIG_ESP_BROWNOUT_DET=y
CONFIG_ESP_BROWNOUT_DET_LVL_SEL_0=y
CONFIG_ESP_BROWNOUT_DET_LVL=0
